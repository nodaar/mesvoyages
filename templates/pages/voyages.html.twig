{% extends "basefront.html.twig" %}

{% block body %}
    <table class="table table-striped">
        <thead>
            <tr>
                <th class="text-left" scope="col">
                    Ville
                    <a href="{{ path('voyage.sort', {champ:'ville', ordre:'ASC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true"><</a>
                    <a href="{{ path('voyage.sort', {champ:'ville', ordre:'DESC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true">></a>                   
                </th>
                <th class="text-left" scope="col">
                    Pays
                    <a href="{{ path('voyage.sort', {champ:'pays', ordre:'ASC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true"><</a>
                    <a href="{{ path('voyage.sort', {champ:'pays', ordre:'DESC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true">></a>
                </th>
                <th class="text-right" scope="col">
                    Note
                    <a href="{{ path('voyage.sort', {champ:'note', ordre:'ASC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true"><</a>
                    <a href="{{ path('voyage.sort', {champ:'note', ordre:'DESC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true">></a>
                </th>
                <th class="text-center" scope="col">
                    Date
                    <a href="{{ path('voyage.sort', {champ:'datecreation', ordre:'ASC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true"><</a>
                    <a href="{{ path('voyage.sort', {champ:'datecreation', ordre:'DESC'}) }}" class="btn btn-primary btn-sm " role="button" aria-pressed="true">></a>
                </th>
            </tr>
        </thead>
        <tbody>
            {% for visite in visites %}
                <tr>
                    <td>
                        <h5 class="text-primary">
                            <a href="#">{{ visite.ville }}</a>
                        </h5>
                    </td>
                    <td>
                        {{ visite.pays }}
                    </td>
                    <td class="text-right">
                        <strong>
                            {% if visite.note < 10 %}
                                <div class="text-danger">
                                    {{ visite.note }}/20
                                </div>
                            {% else %}
                                <div class="text-success">
                                    {{ visite.note }}/20
                                </div>
                            {% endif %}
                        </strong>
                    </td>
                    <td class="text-center">
                        {{ visite.datecreationstring }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
